@model IEnumerable<HEAPIFY_Manager_540.Models.AllergyType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AllergyTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AllergyTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AllergyTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AllergyTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AllergyTypeID })
        </td>
    </tr>
}

</table>
