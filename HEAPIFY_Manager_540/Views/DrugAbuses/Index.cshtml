@model IEnumerable<HEAPIFY_Manager_540.Models.DrugAbuse>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DrugAbuseType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DrugAbuseType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DrugAbuseID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DrugAbuseID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DrugAbuseID })
        </td>
    </tr>
}

</table>
