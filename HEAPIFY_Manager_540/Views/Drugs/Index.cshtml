@model IEnumerable<HEAPIFY_Manager_540.Models.Drug>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DrugName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Dose)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HowLong)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DrugName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Dose)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HowLong)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DrugID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DrugID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DrugID })
        </td>
    </tr>
}

</table>
