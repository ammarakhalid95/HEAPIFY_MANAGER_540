@model IEnumerable<HEAPIFY_Manager_540.Models.Immunization>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Vaccine)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Vaccine)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ImmunizationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ImmunizationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ImmunizationID })
        </td>
    </tr>
}

</table>
