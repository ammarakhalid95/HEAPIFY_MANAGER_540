@model IEnumerable<HEAPIFY_Manager_540.Models.Modify>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ModifyType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ModifyType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ModifyID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ModifyID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ModifyID })
        </td>
    </tr>
}

</table>
