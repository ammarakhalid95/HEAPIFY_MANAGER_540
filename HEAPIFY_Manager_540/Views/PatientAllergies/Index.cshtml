@model IEnumerable<HEAPIFY_Manager_540.Models.PatientAllergy>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Patient.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AllergiesID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Patient.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AllergiesID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PatientAllergyID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PatientAllergyID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PatientAllergyID })
        </td>
    </tr>
}

</table>
