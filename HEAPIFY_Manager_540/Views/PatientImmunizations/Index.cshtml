@model IEnumerable<HEAPIFY_Manager_540.Models.PatientImmunization>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Immunization.Vaccine)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Patient.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateGiven)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Immunization.Vaccine)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Patient.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateGiven)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PatientImmunizationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PatientImmunizationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PatientImmunizationID })
        </td>
    </tr>
}

</table>
