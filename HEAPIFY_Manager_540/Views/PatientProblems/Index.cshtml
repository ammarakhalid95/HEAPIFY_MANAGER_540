@model IEnumerable<HEAPIFY_Manager_540.Models.PatientProblem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Patient.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Problem.ProblemsName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Patient.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Problem.ProblemsName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PatientProblemID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PatientProblemID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PatientProblemID })
        </td>
    </tr>
}

</table>
