@model IEnumerable<HEAPIFY_Manager_540.Models.PhoneNumber>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PhoneType.PhoneTypeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PhoneNumber1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PhoneType.PhoneTypeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PhoneNumber1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PhoneNumberID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PhoneNumberID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PhoneNumberID })
        </td>
    </tr>
}

</table>
